***************
*** 38,56 ****
  import org.apache.ambari.server.controller.spi.Resource;
  
  /**
-  * Service responsible for handling REST requests for the /clusters/{cluster_name}/credentials endpoint.
   */
  public class CredentialService extends BaseService {
  
-   private final String clusterName;
  
-   public CredentialService(ApiVersion apiVersion, String clusterName) {
      super(apiVersion);
-     this.clusterName = clusterName;
    }
  
    /**
-    * Handles: GET  /clusters/{cluster_name}/credentials
     * Get all credentials.
     *
     * @param headers http headers
--- 38,56 ----
  import org.apache.ambari.server.controller.spi.Resource;
  
  /**
+  * Service responsible for handling REST requests for the /clusters/{cluster_id}/credentials endpoint.
   */
  public class CredentialService extends BaseService {
  
+   private final Long clusterId;
  
+   public CredentialService(ApiVersion apiVersion, Long clusterId) {
      super(apiVersion);
+     this.clusterId = clusterId;
    }
  
    /**
+    * Handles: GET  /clusters/{cluster_id}/credentials
     * Get all credentials.
     *
     * @param headers http headers
